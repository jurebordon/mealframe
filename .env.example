# MealFrame Environment Configuration
# Copy this file to .env and customize values as needed

# =============================================================================
# Database Configuration
# =============================================================================

# PostgreSQL connection string
# Format: postgresql+asyncpg://user:password@host:port/database
# Default uses docker-compose service name 'db' as host
DATABASE_URL=postgresql+asyncpg://mealframe:password@db:5432/mealframe

# PostgreSQL password (used by docker-compose)
DB_PASSWORD=password

# =============================================================================
# API Configuration
# =============================================================================

# CORS allowed origins (comma-separated)
# For development, localhost:3000 is the Next.js frontend
# Note: Backend runs on port 8003
CORS_ORIGINS=http://localhost:3000

# API metadata
API_TITLE=MealFrame API
API_DESCRIPTION=Meal planning API that eliminates decision fatigue
API_VERSION=0.1.0

# =============================================================================
# Development Settings
# =============================================================================

# Enable debug mode (enables SQL query logging)
# WARNING: Do not enable in production as it logs sensitive data
DEBUG=false

# =============================================================================
# AI Onboarding (ADR-015)
# =============================================================================

# Anthropic API key for Claude Sonnet (setup generation + meal chat)
ANTHROPIC_API_KEY=

# USDA FoodData Central API key for nutrition lookup
# Get one free at https://fdc.nal.usda.gov/api-key-signup.html
USDA_API_KEY=

# =============================================================================
# Notes
# =============================================================================

# - This file is a template and should not contain real secrets
# - Copy to .env and customize for local development
# - Never commit .env to version control (it's in .gitignore)
# - All settings are optional and have defaults in app/config.py
# - Environment variables override .env file values
